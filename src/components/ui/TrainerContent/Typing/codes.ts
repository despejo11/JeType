export const codes: { [key: string]: string } = {
  javascript: `setTimeout(() => { let text1 = 'Quick brown fox'; let text2 = 'jumps over lazy dog'; for (let i = 0; i < 5; i++) { let before = text1 + ' and ' + text2 + '.'; console.log(before)`,
}
